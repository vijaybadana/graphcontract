/**
 * The projected React Flow box and the rendered ContractNode shell must use
 * one baseline size. Interaction, subgraph containment, and layout-facing
 * application logic all consume these values rather than maintaining local
 * approximations.
 */
export const CONTRACT_NODE_WIDTH = 220;
export const CONTRACT_NODE_HEIGHT = 134;
